# #Mindwm API
#
##This document describes the documentation, a collection of JSON schemas and example cloudevent and payloads
#
#The version of the OpenAPI document: 0.1.0
#
#Generated by: https://openapi-generator.tech
#Generator version: 7.7.0
#

require "big"
require "json"
require "time"

module OpenAPIClient
  class ClipboardPayload
    include JSON::Serializable

    # Optional properties
    # Starting position of clipboard selection [x,y]
    @[JSON::Field(key: "start", type: Array(Int32)?, nillable: true, emit_null: false)]
    property start : Array(Int32)?

    # Ending position of clipboard selection [x,y]
    @[JSON::Field(key: "stop", type: Array(Int32)?, nillable: true, emit_null: false)]
    property stop : Array(Int32)?

    # Clipboard data
    @[JSON::Field(key: "data", type: String?, nillable: true, emit_null: false)]
    property data : String?

    # Clipboard type
    @[JSON::Field(key: "type", type: String?, nillable: true, emit_null: false)]
    property _type : String?

    @[JSON::Field(key: "context", type: ClipboardPayloadContext?, nillable: true, emit_null: false)]
    property context : ClipboardPayloadContext?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@start : Array(Int32)?, @stop : Array(Int32)?, @data : String?, @_type : String?, @context : ClipboardPayloadContext?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@start.nil? && @start.size > 2
        invalid_properties.push("invalid value for \"start\", number of items must be less than or equal to 2."
      end

      if !@start.nil? && @start.size < 2
        invalid_properties.push("invalid value for \"start\", number of items must be greater than or equal to 2."
      end

      if !@stop.nil? && @stop.size > 2
        invalid_properties.push("invalid value for \"stop\", number of items must be less than or equal to 2."
      end

      if !@stop.nil? && @stop.size < 2
        invalid_properties.push("invalid value for \"stop\", number of items must be greater than or equal to 2."
      end

      if !@data.nil? && @data.to_s.size < 1
        invalid_properties.push("invalid value for \"data\", the character length must be great than or equal to 1.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@start.nil? && @start.size > 2
      return false if !@start.nil? && @start.size < 2
      return false if !@stop.nil? && @stop.size > 2
      return false if !@stop.nil? && @stop.size < 2
      return false if !@data.nil? && @data.to_s.size < 1
      _type_validator = EnumAttributeValidator.new("String", ["primary", "secondary", "clipboard"])
      return false unless _type_validator.valid?(@_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] start Value to be assigned
    def start=(start)
      if !start.nil? && start.size > 2
        raise ArgumentError.new("invalid value for \"start\", number of items must be less than or equal to 2.")
      end

      if !start.nil? && start.size < 2
        raise ArgumentError.new("invalid value for \"start\", number of items must be greater than or equal to 2.")
      end

      @start = start
    end

    # Custom attribute writer method with validation
    # @param [Object] stop Value to be assigned
    def stop=(stop)
      if !stop.nil? && stop.size > 2
        raise ArgumentError.new("invalid value for \"stop\", number of items must be less than or equal to 2.")
      end

      if !stop.nil? && stop.size < 2
        raise ArgumentError.new("invalid value for \"stop\", number of items must be greater than or equal to 2.")
      end

      @stop = stop
    end

    # Custom attribute writer method with validation
    # @param [Object] data Value to be assigned
    def data=(data)
      if !data.nil? && data.to_s.size < 1
        raise ArgumentError.new("invalid value for \"data\", the character length must be great than or equal to 1.")
      end

      @data = data
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidator.new("String", ["primary", "secondary", "clipboard"])
      unless validator.valid?(_type)
        raise ArgumentError.new("invalid value for \"_type\", must be one of #{validator.allowable_values}.")
      end
      @_type = _type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.same?(o)
      self.class == o.class &&
          start == o.start &&
          stop == o.stop &&
          data == o.data &&
          _type == o._type &&
          context == o.context
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [start, stop, data, _type, context].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end

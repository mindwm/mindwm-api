/**
 * Mindwm API
 * This document describes the documentation, a collection of JSON schemas and example cloudevent and payloads
 *
 * OpenAPI spec version: 0.1.0
 *
 * Contact: team@openapitools.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package MindwWM.model
import MindwWM.model.oas_any_type_not_mapped
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*

/** TmuxPaneIoDocumentData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class TmuxPaneIoDocumentData(
/* User input */
  input: String,

  /* Command output (mix of stdout & stderr) */
  output: String,

  /* ps1 (prompt) AFTER the input and output */
  ps1: String

  ) {

  def asJson: String = write(this)

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val errors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ==================
        // input



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if input == null then 0 else input.length
            if (len < 1) {
               errors += ValidationError(path :+ TmuxPaneIoDocument.Fields.input, s"length $len is shorter than the min length 1")
            }
        }



        







        

        // ==================
        // output



        // validate min length 0
        if (errors.isEmpty || !failFast)  {
          val len = if output == null then 0 else output.length
            if (len < 0) {
               errors += ValidationError(path :+ TmuxPaneIoDocument.Fields.output, s"length $len is shorter than the min length 0")
            }
        }



        







        

        // ==================
        // ps1



        // validate min length 1
        if (errors.isEmpty || !failFast)  {
          val len = if ps1 == null then 0 else ps1.length
            if (len < 1) {
               errors += ValidationError(path :+ TmuxPaneIoDocument.Fields.ps1, s"length $len is shorter than the min length 1")
            }
        }



        







        

    errors.toSeq
  }

  def validated(failFast : Boolean = false) : scala.util.Try[TmuxPaneIoDocument] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : TmuxPaneIoDocument = {
    TmuxPaneIoDocument(
        input = 
        input
        
        ,
        output = 
        output
        
        ,
        ps1 = 
        ps1
        
        
    )
  }
}

object TmuxPaneIoDocumentData {

  given readWriter : RW[TmuxPaneIoDocumentData] = macroRW

  def fromJsonString(jason : String) : TmuxPaneIoDocumentData = try {
        read[TmuxPaneIoDocumentData](jason)
     } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
     }

  def manyFromJsonString(jason : String) : Seq[TmuxPaneIoDocumentData] = try {
        read[List[TmuxPaneIoDocumentData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[TmuxPaneIoDocument]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[TmuxPaneIoDocument]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, TmuxPaneIoDocumentData] = try {
        read[Map[String, TmuxPaneIoDocumentData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, TmuxPaneIoDocument]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, TmuxPaneIoDocument]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

